aiogram==3.13.1
openai>=1.50.0
chromadb==0.5.23
sentence-transformers==3.3.1
python-dotenv==1.0.1
# aiogram 3.13.1 pins pydantic<2.10; staying at 2.9.x avoids a forced
# bump of aiogram (which sometimes changes Dispatcher / FSM behaviour).
pydantic==2.9.2
pydantic-settings==2.6.1
SQLAlchemy==2.0.36
aiosqlite==0.20.0
# Postgres driver — uncomment for prod with >50 concurrent WhatsApp chats.
# SQLite single-writer becomes the bottleneck past that point; switch the
# DATABASE_URL to postgresql+asyncpg://... and uncomment this line.
# asyncpg>=0.29.0
loguru==0.7.3
aiofiles==24.1.0
python-docx>=1.1.0
pdfminer.six>=20221105
telegraph>=2.2.0
flask>=3.0.0
aiohttp>=3.9.0
anthropic>=0.60.0
